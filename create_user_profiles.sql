CREATE TABLE IF NOT EXISTS user_profiles (id SERIAL PRIMARY KEY, name TEXT NOT NULL, interests TEXT[] NOT NULL, projects TEXT[] NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP);
